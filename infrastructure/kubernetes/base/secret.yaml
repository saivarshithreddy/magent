# ============================================================================
# SECRET - Placeholder for sensitive values
# NOTE: Replace literals via kustomize secretGenerator or external secret store.
# ============================================================================
apiVersion: v1
kind: Secret
metadata:
  name: research-assistant-secrets
  namespace: research-assistant
  labels:
    app.kubernetes.io/name: research-assistant
type: Opaque
stringData:
  # Replace with real values or configure secretGenerator; these are placeholders.
  OLLAMA_API_KEY: "replace-me"
  LANGCHAIN_API_KEY: "replace-me"
